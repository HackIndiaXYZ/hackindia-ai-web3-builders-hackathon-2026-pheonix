# Succession and recovery

A nominee is dormant until a controlled succession case is verified. Evidence
alone does not change ownership. A registrar verifies the case; successor
activation additionally requires the nominee to have an active credential.
Key recovery is different: it preserves ownership while revoking the old
credential and requiring a new wallet link.
