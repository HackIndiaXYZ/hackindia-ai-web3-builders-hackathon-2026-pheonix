# Title history and public verification

The map leads to the parcel record, which separates operational title history
from immutable chain evidence. Public verification exposes only a verification
identifier, status, title-health score, history count and anchoring state. It
does not expose identities, wallets, documents or internal risk notes.
